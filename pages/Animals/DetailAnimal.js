import { Text } from "react-native"

export default DetailAnimal = ({animal}) => {
  // quel paramètre envoyé, l'ID ?
  // récupérer les infos de l'animal avec react-query ?

  return <Text>{animal}</Text>
}